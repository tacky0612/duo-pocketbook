# セキュリティポリシー

## サポート対象

本プロジェクトは `main` ブランチの最新状態のみをサポートします。セキュリティ修正は `main` に対して行い、過去のコミット・タグへのバックポートは行いません。

| バージョン | サポート |
|---|---|
| `main`（最新） | ✅ |
| それ以前 | ❌ |

## 想定利用範囲（スコープ）

duo-pocketbook は**2アカウント固定・環境変数ベースの認証**で運用する私的用途の家計簿アプリです。以下を前提に設計されています。

- 認証は環境変数で設定した2ユーザー（bcrypt ハッシュ）+ JWT。マルチテナントや一般公開向けの認証基盤ではありません。
- 本番運用時は必ず `JWT_SECRET` を十分に強いランダム値にし、`terraform.tfvars` などの機密値をコミットしないでください（`.gitignore` 済み）。
- 公開デモ（GitHub Pages のデモモード）はブラウザ内の localStorage で完結し、サーバーへデータを送信しません。

第三者が本番運用する場合、上記の前提を理解したうえで設定してください。

## 脆弱性の報告

**脆弱性は公開 Issue で報告しないでください。**

GitHub の **Private vulnerability reporting** を利用してください。

1. 本リポジトリの **Security** タブを開く
2. **Report a vulnerability** から非公開で報告する

報告には、可能な範囲で以下を含めてください。

- 影響を受ける箇所（ファイル・エンドポイント・機能）
- 再現手順または PoC
- 想定される影響（情報漏洩・認証バイパス・DoS など）
- 既知の場合は修正案

## 対応の目安

- 受領の確認: 数日以内を目標
- 深刻度の評価と修正方針の共有: 状況に応じて随時
- 修正のリリース後、報告者の希望に応じて謝辞を記載します

個人で運用しているプロジェクトのため対応にお時間をいただく場合があります。ご理解ください。
